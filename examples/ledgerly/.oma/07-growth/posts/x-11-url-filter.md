<!-- Pillar: How it works
     Day 12 · X/Twitter · single post
     Rests on: REQ-007 as built — the status filter is reflected in the URL and survives
       reload; QA run-3 drove it in a browser (clicking Unpaid → /invoices?status=unpaid,
       no full reload; ?status=paid filtered correctly on reload).
     Asset: A2 (invoice list, unpaid filter active, URL bar visible in the capture).
     Drafted by OMA. Nothing is posted for you. -->

# X — day 12

## Post — attach asset A2

```
Small thing I'm unreasonably pleased with: the invoice filter lives in the URL.

/invoices?status=unpaid

Bookmark it. It survives a reload. It's the only page I actually open — the list
of people who owe me money.
```

Fits 280 as written; recount if you edit.

## Asset

**A2** — the unpaid-filtered list **with the browser address bar in frame**. The
URL is the whole point of the post; cropping it out kills the screenshot.

## Before you publish

- This is a small post on purpose. Not every day needs a lesson attached.
- Same screenshot-honesty rule as day 2: real account with edited-in-app
  placeholder names, or an openly-labelled demo account. Client names in a
  public screenshot are somebody else's business, not yours to publish.
