# PRD — {{project_name}}

> Written by oma-project-manager. Requirement IDs are stable for the life of the
> project — never renumber, never reuse. Every task in 04-build/tasks.json must
> cite one of these IDs. A task that cites nothing is scope creep.

## Problem

<!-- 2-4 sentences. Whose problem, how painful, what they do today instead. -->

## Solution in one paragraph

<!-- What we're building and why it beats the status quo for the persona. -->

## Requirements

<!-- One block per requirement. `priority` uses MoSCoW: must / should / could.
     v1 ships MUSTs only unless the user says otherwise. Each acceptance
     criterion must be checkable by a command or by loading a page — no vibes. -->

### REQ-001 — {{title}}
- **Priority:** must
- **Persona:** {{which persona feels this}}
- **Story:** As a {{persona}}, I want {{capability}} so that {{outcome}}.
- **Acceptance:**
  - [ ] {{observable, testable criterion}}
  - [ ] {{another}}
- **Notes:** {{constraints, edge cases, open questions}}

## Out of scope (v1)

<!-- Explicit non-goals with one-line reasons. This section prevents more
     wasted work than any other in the document. -->

| Not doing | Why not |
|---|---|
| {{feature}} | {{reason}} |

## Deferred

<!-- Good ideas that survive to v2. Park them here so they stop coming up. -->

## Riskiest assumptions

<!-- The 2-3 assumptions that, if wrong, invalidate the most work. -->
